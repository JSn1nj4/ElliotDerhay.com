<x-dynamic-component
    :component="$getFieldWrapperView()"
    :id="$getId()"
    :label="$getLabel()"
    :label-sr-only="$isLabelHidden()"
    :helper-text="$getHelperText()"
    :hint="$getHint()"
    :hint-action="$getHintAction()"
    :hint-color="$getHintColor()"
    :hint-icon="$getHintIcon()"
    :required="$isRequired()"
    :state-path="$getStatePath()"
>
	@if($modelHasImage())
		<img x-data="{ state: $wire.entangle('{{ $getStatePath() }}').defer }" src="{{ $getImage()->url }}" alt="">
	@endif
</x-dynamic-component>
